---
interface Props {
  src: string;
  alt: string;
  class?: string;
}

const { src, alt, class: className = '' } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class={`h-full w-full object-cover object-top ${className}`}
  width="1080"
  height="1920"
  loading="lazy"
  decoding="async"
/>
